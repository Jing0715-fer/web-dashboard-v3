# Iteration Backlog

One item per round (new-file tasks only — docs/scripts/templates, never edits
to existing code). The auto-iter service marks items `[x]` as it completes
and pushes them. Append new items at the bottom.

- [ ] docs-api — Write `docs/API.md`: a complete REST API reference for this project. List every route under `src/app/api/**/route.ts` with method, auth requirement (public / approved user / admin), request params/body and response shape, based on each route's header comment and code. Group by domain (auth, projects, environments, devices, llm, harness, mesh, admin).
- [ ] docs-architecture — Write `docs/ARCHITECTURE.md`: system architecture overview of this repo — Next.js 16 App Router structure, the 10k-line page.tsx surface, lib layer (auth/session store, llm gateway, harness engine, remote sync, mesh relay), Prisma/SQLite data model (User/Session/Device/Project/Environment/ActivityEvent/LlmConfig), the mini-services agents (Windows/macOS/Linux) and how they register/heartbeat, and the dev-mode runtime topology.
- [ ] docs-agent-setup — Write `docs/AGENT-SETUP.md`: step-by-step guide for registering a remote machine (Windows focus): where agents live (mini-services/agent-win), how start.bat/port/apiKey work, agent-config.json and dashboardDb wiring, heartbeat + project sync, mesh relay auto-registration, and troubleshooting (duplicate agents, missing dashboardDb, repoUrl sync).
- [ ] docs-operations — Write `docs/OPERATIONS.md`: operator runbook — dev server lifecycle (bun run dev, dev.log locations), database (db/custom.db, prisma db:push), LLM configuration flow, the auto-iter service (port 3111, state.json, backlog.md, pause/resume), worklog.md conventions, and common failure modes with fixes.
- [ ] docs-security — Write `docs/SECURITY.md`: security notes — session tokens (HMAC-signed + Session store rows, revocation), admin vs approved user guards on routes, API-key masking (masked round-trips, useSavedKey), agent apiKey handling, PAT hygiene for auto-iter push (rotate after use), and recommended rotation practices.
- [ ] scripts-health-check — Write `scripts/health-check.sh`: a bash one-shot health probe — checks dev server (curl /api/auth/session expects 200/401), auto-iter status (curl localhost:3111), agent processes (pgrep bun/agent), disk usage of db/ and dev.log size, and prints a compact traffic-light summary (OK/WARN/FAIL per check). Make it executable.
- [ ] docs-troubleshooting — Write `docs/TROUBLESHOOTING.md`: symptom→cause→fix table for the known issues this project actually hit: EADDRINUSE double dev server, Turbopack cold-compile slowness after git reset, LLM settings dialog slow first open, re-fetch env no-response on remote projects, Windows duplicate agents (two PIDs), repoUrl not syncing across machines, and SQLite "database is occupied" errors.
- [ ] github-templates — Create `.github/ISSUE_TEMPLATE/bug_report.md` and `.github/ISSUE_TEMPLATE/feature_request.md` (plus a short `.github/PULL_REQUEST_TEMPLATE.md`): markdown templates tailored to this project (bug: which machine — dashboard/agent/both, browser, dev.log excerpt; feature: which layer — UI/API/lib/agent).
